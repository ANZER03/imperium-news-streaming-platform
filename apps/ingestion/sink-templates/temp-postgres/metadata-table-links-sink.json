{
  "name": "${TEMP_SINK_METADATA_LINKS_NAME}",
  "config": {
    "connector.class": "io.debezium.connector.jdbc.JdbcSinkConnector",
    "tasks.max": "1",
    "topics": "imperium.metadata.public.table_links",
    "connection.url": "jdbc:postgresql://${TARGET_PG_HOST}:${TARGET_PG_PORT}/${TARGET_PG_DATABASE}",
    "connection.username": "${TARGET_PG_USER}",
    "connection.password": "${TARGET_PG_PASSWORD}",
    "insert.mode": "upsert",
    "primary.key.mode": "record_key",
    "primary.key.fields": "id",
    "delete.enabled": "false",
    "schema.evolution": "none",
    "table.name.format": "sink_meta_links",
    "quote.identifiers": "false",
    "consumer.override.auto.offset.reset": "latest",
    "transforms": "unwrap,project",
    "transforms.unwrap.type": "io.debezium.transforms.ExtractNewRecordState",
    "transforms.unwrap.drop.tombstones": "true",
    "transforms.unwrap.delete.handling.mode": "drop",
    "transforms.project.type": "org.apache.kafka.connect.transforms.ReplaceField$$Value",
    "transforms.project.include": "id,link,authority_id,title,langue_id,theme_id,rubrique_id,pays_id,news,rss,actif,lastchecked"
  }
}
