apiVersion: 1

providers:
  - name: obs-dashboards
    orgId: 1
    folder: Observability
    folderUid: obs-folder
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
